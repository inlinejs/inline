{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "package": {
      "inputs": ["/packages/*"],
      "outputs": []
    }
  }
}
